extends layout

block content
  div(id="main-container")
    div(id="input-top-text") Input a url to shorten it
    div(id="input-container")
      input(id="main-input" type="url" placeholder="url")
      button(id="submit-btn" type="submit") shorten
